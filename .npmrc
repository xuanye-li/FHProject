shamefully-hoist=true                    # Hoist all dependencies (legacy compatibility)
strict-peer-dependencies=false           # Skip peer dependency warnings
ignore-workspace-root-check=true         # Allow installing deps in root